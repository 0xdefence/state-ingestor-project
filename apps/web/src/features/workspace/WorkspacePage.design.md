# Workspace surface

Mode: Operate. Success is uploading a CSV, seeing processing feedback, finding outstanding review and failed runs, and opening evidence within an explicit file scope.

Visual authority: docs/ARCHITECTURE.md section 8 and the approved revision-9 mockup; architecture overrides the mockup's obsolete charts. This implementation uses the centralized tokens in styles/tokens.css and the root PRODUCT.md context. Do not introduce a new direction during later run/review work.
